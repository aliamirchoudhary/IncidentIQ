{
  "name": "tests",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:local": "BASE_URL=http://localhost:8787 vitest run",
    "test:production": "BASE_URL=https://core-api.aliamirchoudhary.workers.dev vitest run",
    "test:unit": "vitest run unit"
  },
  "dependencies": {
    "shared": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250701.0",
    "vitest": "^3.1.3",
    "typescript": "^5.7.0"
  }
}
